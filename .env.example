# 数据库配置
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/loclock

# JWT配置
SECRET_KEY=your-secret-key-please-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS配置（多个源用逗号分隔）
# BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# 应用配置
PROJECT_NAME=LocLock
VERSION=1.0.0
ENVIRONMENT=development

# 短链接配置
SHORT_CODE_LENGTH=6

# 2FA加密配置
# 使用以下命令生成加密密钥：
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOTP_ENCRYPTION_KEY=your-encryption-key-please-change-this-in-production

